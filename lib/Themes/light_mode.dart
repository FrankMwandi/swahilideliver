// ignore_for_file: prefer_const_constructors

import 'package:flutter/material.dart';

ThemeData lightmode = ThemeData(
  colorScheme: ColorScheme.light(
    background: Colors.grey[300] ?? Color(0xFFE0E0E0),
    primary: Colors.grey[500] ?? Color(0xFF9E9E9E),
    secondary: Colors.grey[100] ?? Color(0xFFFAFAFA),
    tertiary: Colors.white,
    inversePrimary: Colors.grey[700] ?? Color(0xFF616161),
  ),
);

